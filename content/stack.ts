import {SkillsType} from '@/content/types';
import skills from '@/content/skills';

const stack: SkillsType = {
  comment: "// Skills",
  title: "{Tech Stack}",
  statement: "Always learning, always shipping.",
  skills: skills,
};

export default stack;