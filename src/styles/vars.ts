import { VarsConfig } from "./types";
import colors from "./colors";

const vars = {
  rootSize: 20,
  responsiveMin: 20,
  responsiveMax: 70,
  colors: colors,
  font: {
    primary: "Roboto, serif",
    secondary: "Roboto, serif",
  },
} as VarsConfig;

export default vars;
