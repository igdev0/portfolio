# Plan of Action

# Phase 1 — Build portfolio (NOW)

- Build components normally
- Use TypeBox for props
- Keep props serializable
- Don’t over-engineer yet

# Phase 2 — Introduce schema renderer

```ts 
renderNode
```

→ renders components from JSON

Use it inside your app, not as a tool yet

# Phase 3 — Add persistence

- Save page structure as JSON
- Load it dynamically

Now you already have:
👉 a backend-driven UI system

# Phase 4 — Build playground/editor

Only now:
- drag & drop
- prop editors
- live preview